-- Team members --
Hugo Brunie
Yvon Garbage

-- Generating drive.ll (driver/src/drive.ll)

Use the Makefile :
    make driver/src/drive.ll

-- Generating enseirBOT.so --

Use the Makefile :
   make all

-- Copying enseirbBOT.so into torcs directory --

Use the Makefile :
    make torcs-1.3.5/src/drivers/enseirBOT/enseirBOT.so